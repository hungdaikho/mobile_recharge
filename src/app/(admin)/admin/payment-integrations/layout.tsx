"use client";
import React from "react";

const PaymentIntegrationsLayout = ({ children }: { children: React.ReactNode }) => {
    return <div className="bg-gray-100 min-h-screen">{children}</div>;
};

export default PaymentIntegrationsLayout;
